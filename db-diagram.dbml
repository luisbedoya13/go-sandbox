Project project_name {
  database_type: 'DynamoDB'
  Note: 'App de finanzas'
}

Table users {
  id ULID [primary key]
  email text [note: 'partition key']
  password text
}

Table accounts {
  id ULID [primary key]
}

Table pockets {
  id ULID [primary key]
  account_id ULID [ref: < accounts.id]
}